import React from "react";
// export Widget, takes and event object then returns a jsx element named tito-widget with and
// event prop set to the value of the passed in event object
export const Widget = ({ event }) => <tito-widget event={event} />;
